PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USER=juanm_necotur
DB_PASSWORD=
DB_NAME=juanm_necotur
SESSION_SECRET=anasherecontraremilashe
NODE_ENV=development

# Carpeta del sitio estático (HTML, CSS, img). Por defecto: ../public_html (hermano de server/)
# En desarrollo local, si aún usás WireFrame: PUBLIC_HTML_PATH=../WireFrame
PUBLIC_HTML_PATH=

# URL pública opcional (ej. https://tudominio.com) — prefijo para rutas de imagen en BD
PUBLIC_BASE_URL=

# Tamaño máximo de imagen en MB
MAX_IMAGE_SIZE_MB=10
